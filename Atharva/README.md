# Event Ticketing API

High-concurrency Event Ticketing API with Firebase and Swagger.

## Setup

1. Copy `.env.example` to `.env`
2. Add `serviceAccountKey.json` from Firebase Console.
3. Run `npm install`
4. Run `npm run dev`
